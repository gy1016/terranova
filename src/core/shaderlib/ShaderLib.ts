import common from "./common.glsl";
import log_depth_vert from "./log_depth_vert.glsl";
import ray_intersects_sphere from "./ray_intersects_sphere.glsl";

export const ShaderLib = {
  common,
  log_depth_vert,
  ray_intersects_sphere,
};
